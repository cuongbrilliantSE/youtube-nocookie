diff --git a/node_modules/yt-search/dist/yt-search.js b/node_modules/yt-search/dist/yt-search.js
index 3e65db0..60bf3ff 100644
--- a/node_modules/yt-search/dist/yt-search.js
+++ b/node_modules/yt-search/dist/yt-search.js
@@ -278,11 +278,25 @@ function getSearchResults(_options, callback) {
   }
   var uri = TEMPLATES.SEARCH_DESKTOP + queryString;
   var params = _url.parse(uri);
+  var REGION_IPS = {
+    US: '8.8.8.8',
+    JP: '133.242.0.1',
+    KR: '110.45.0.1',
+    GB: '151.236.0.1',
+    DE: '138.201.0.1',
+    FR: '51.15.0.1',
+    TH: '171.96.0.1',
+    VN: '118.69.0.1'
+  };
+  var clientIp = REGION_IPS[gl] || '8.8.8.8';
+
   params.headers = {
     'user-agent': _userAgent,
     'accept': 'text/html',
     'accept-encoding': 'gzip',
-    'accept-language': 'en-US'
+    'accept-language': (hl ? hl + '-' + gl + ',' + hl + ';q=0.9,' : '') + 'en-US;q=0.8',
+    'x-forwarded-for': clientIp,
+    'client-ip': clientIp
   };
   debug(params);
   debug('getting results: ' + _options.currentPage);
@@ -452,7 +466,7 @@ function _parseSearchResultInitialData(responseText, callback) {
 
             // publish/upload date
             var agoText = _jp.value(item, '$..publishedTimeText..text') || _jp.value(item, '$..publishedTimeText..simpleText');
-            var viewCountText = _jp.value(item, '$..viewCountText..text') || _jp.value(item, '$..viewCountText..simpleText') || "0";
+            var viewCountText = String(_jp.value(item, '$..viewCountText..text') || _jp.value(item, '$..viewCountText..simpleText') || "0");
             var viewsCount = Number(viewCountText.split(/\s+/)[0].split(/[,.]/).join('').trim());
             var lengthText = _jp.value(item, '$..lengthText..text') || _jp.value(item, '$..lengthText..simpleText');
             var duration = _parseDuration(lengthText || '0:00');
@@ -465,7 +479,7 @@ function _parseSearchResultInitialData(responseText, callback) {
               type: 'video',
               videoId: videoId,
               url: url,
-              title: title.trim(),
+              title: (typeof title === 'string' ? title : String(title || '')).trim(),
               description: description,
               image: thumbnail,
               thumbnail: thumbnail,
@@ -530,7 +544,7 @@ function _parseSearchResultInitialData(responseText, callback) {
               type: 'list',
               listId: listId,
               url: _url2,
-              title: _title.trim(),
+              title: (typeof _title === 'string' ? _title : String(_title || '')).trim(),
               image: _thumbnail,
               thumbnail: _thumbnail,
               videoCount: Number(_parseNumbers(video_count_label)[0]) || video_count,
@@ -549,7 +563,7 @@ function _parseSearchResultInitialData(responseText, callback) {
             var _author_name2 = _jp.value(item, '$..shortBylineText..text') || _jp.value(item, '$..longBylineText..text') || _jp.value(item, '$..displayName..text') || _jp.value(item, '$..displayName..simpleText');
             var about_channel = _jp.query(item, '$..channelRenderer..descriptionSnippet..text').join('') || '';
             var _video_count_label = _jp.value(item, '$..videoCountText..simpleText') || _jp.value(item, '$..videoCountText..label') || _jp.value(item, '$..videoCountText..text') || '0';
-            var channel_verified_label = _jp.value(item, '$..channelRenderer..ownerBadges..style') || _jp.value(item, '$..channelRenderer..ownerBadges..tooltip') || _jp.value(item, '$..channelRenderer..ownerBadges..label') || '';
+            var channel_verified_label = String(_jp.value(item, '$..channelRenderer..ownerBadges..style') || _jp.value(item, '$..channelRenderer..ownerBadges..tooltip') || _jp.value(item, '$..channelRenderer..ownerBadges..label') || '');
             var channel_verified =
             // has "verified" or "_verified" text in it
             channel_verified_label.toLowerCase().trim().search(/[\s_]?verified/) >= 0;
@@ -579,7 +593,7 @@ function _parseSearchResultInitialData(responseText, callback) {
               url: TEMPLATES.YT + base_url,
               baseUrl: base_url,
               id: _channelId,
-              title: _title2.trim(),
+              title: (typeof _title2 === 'string' ? _title2 : String(_title2 || '')).trim(),
               about: about_channel,
               image: _thumbnail2,
               thumbnail: _thumbnail2,
@@ -611,7 +625,7 @@ function _parseSearchResultInitialData(responseText, callback) {
               type: 'live',
               videoId: videoId,
               url: _url3,
-              title: _title3.trim(),
+              title: (typeof _title3 === 'string' ? _title3 : String(_title3 || '')).trim(),
               description: _description,
               image: _thumbnail3,
               thumbnail: _thumbnail3,
@@ -966,9 +980,7 @@ function _parsePlaylistLastUpdateTime(lastUpdateLabel) {
   debug('fn: _parsePlaylistLastUpdateTime');
   var DAY_IN_MS = 1000 * 60 * 60 * 24;
   try {
-    // ex "Last Updated on Jun 25, 2018"
-    // ex: "Viimeksi päivitetty 25.6.2018"
-
+    lastUpdateLabel = String(lastUpdateLabel || '');
     var words = lastUpdateLabel.toLowerCase().trim().split(/[\s.-]+/);
     if (words.length > 0) {
       var lastWord = words[words.length - 1].toLowerCase();
